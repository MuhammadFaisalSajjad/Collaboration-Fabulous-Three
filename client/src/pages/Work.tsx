import React from "react";
import Layout from "../layout/Layout";

const Work = (): React.JSX.Element => {
  return (
    <React.Fragment>
      <Layout>
        <div>Work Page</div>
      </Layout>
    </React.Fragment>
  );
};

export default Work;
